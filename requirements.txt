# BTC Quant Research — Dependencies
#
# Core pipeline uses Python stdlib only.
# Optional dependencies for extended functionality.

# Required for live data collection (WebSocket)
websocket-client>=1.6.0

# Required for chart generation
matplotlib>=3.7.0
