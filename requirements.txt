# BTC Quant Research — Dependencies
#
# Core pipeline uses Python stdlib only (csv, math, collections).
# No pandas or numpy required.

# Chart generation
matplotlib>=3.7.0

# Live data collection (WebSocket) — optional, not needed for sample pipeline
websocket-client>=1.6.0
